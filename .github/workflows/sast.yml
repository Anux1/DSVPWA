name: SAST SonarQube

on:
  push:
    branches: [ "main" ]

jobs:
  sonarqube-analysis:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: temurin      # Obligatoire
          java-version: '17'

      - name: Cache SonarQube packages
        uses: actions/cache@v3
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar

      - name: Run SonarQube Scan
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          curl -sSLo sonar-scanner.zip \
            https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-4.8.0.2856-linux.zip
          unzip sonar-scanner.zip
          ./sonar-scanner-*/bin/sonar-scanner \
            -Dsonar.projectKey=tp-sast \
            -Dsonar.sources=. \
            -Dsonar.host.url=http://127.0.0.1:9000 \
<<<<<<< HEAD
<<<<<<< HEAD
            -Dsonar.login=sqa_a85bf755e621f25be4747f3d823364e719ba535a
=======
            -Dsonar.login=$sqa_a85bf755e621f25be4747f3d823364e719ba535a
>>>>>>> bde2a87 (chore:declenchement du pipeline CI)
=======
            -Dsonar.login=sqa_a85bf755e621f25be4747f3d823364e719ba535a
>>>>>>> e98384b (chore:declenchement du pipeline CI)

